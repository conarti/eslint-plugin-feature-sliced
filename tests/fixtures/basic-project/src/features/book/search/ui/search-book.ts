/* Invalid: search and toggle-read are two slices inside a group folder that carries its own public api */
import { toggleRead } from '../../toggle-read/ui/toggle-read';

export const searchBook = `search-${toggleRead}`;

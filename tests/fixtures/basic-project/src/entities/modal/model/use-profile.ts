/* Invalid: neither slice folder carries a public api, so the segment bound is what keeps them apart */
import { profile } from '../../request-profile/model/profile';

export const modalProfile = `modal-${profile}`;

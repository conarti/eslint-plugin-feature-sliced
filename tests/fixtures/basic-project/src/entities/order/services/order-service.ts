/* Valid: services and helpers are two folders of the order slice, which carries a public api */
import { orderHelper } from '../helpers/order-helper';

export const orderService = `order-${orderHelper}`;
